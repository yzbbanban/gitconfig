server:
  port: 7001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/yzbban/didisoace.git
          username:
          password:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/
